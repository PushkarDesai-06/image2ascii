## Image to Ascii converter :)


```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt`
```

### Change the image path in `main.py` file

Run the program : 

```bash
python main.py
```
